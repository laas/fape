instance Item I2,I3;

[start] I2.location := D0;
[start] I3.location := C0;
trans(I2,A0);
trans(I, D0);
trans(I3,A0);

// constant Robot r1;
// constant Robot r2;
// constant NavLocation l1;
// Transport(r1, r2, l1, A0, I2);

// constant Robot r3;
// constant Robot r4;
// constant NavLocation l2;
// Transport(r3, r4, l2, D0, I);

